using System.ComponentModel;

namespace DotNetToolBox
{
    [DefaultValue(Default)]
    public enum TextCasing
    {
        Default = 0,
        Uppercase,
        Lowercase,
        TitleCase
    }
}
